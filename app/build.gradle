plugins {
    id 'com.android.application'
}

android {
    namespace 'com.cinehd.tv'
    compileSdk 34

    defaultConfig {
        applicationId "com.cinehd.tv"
        minSdk 21          // Android 5.0 — covers all modern Android TV devices
        targetSdk 34
        versionCode 1
        versionName "1.0"
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }
}

dependencies {
    implementation 'androidx.appcompat:appcompat:1.6.1'
    implementation 'androidx.leanback:leanback:1.0.0'   // Android TV UI components
}
